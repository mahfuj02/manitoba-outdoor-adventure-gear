# <!-- app/views/shared/_mini_cart.html.erb -->
# <div class="dropdown-menu dropdown-menu-end p-3" style="width: 300px;">
#   <h6 class="dropdown-header">Your Cart</h6>
  
#   <% if session[:cart].blank? || session[:cart].empty? %>
#     <p class="text-muted">Your cart is empty.</p>
#   <% else %>
#     <div class="mini-cart-items">
#       <% session[:cart].each do |product_id, quantity| %>
#         <% product = Product.find_by(id: product_id) %>
#         <% if product %>
#           <div class="mini-cart-item d-flex align-items-center mb-2">
#             <% if product.image.attached? %>
#               <%= image_tag product.image.variant(resize_to_limit: [40, 40]), class: "me-2" %>
#             <% end %>
#             <div class="flex-grow-1">
#               <div><%= product.name %></div>
#               <div class="text-muted"><%= quantity %> × <%= number_to_currency(product.price) %></div>
#             </div>
#           </div>
#         <% end %>
#       <% end %>
#     </div>
#     <div class="d-grid gap-2 mt-3">
#       <%= link_to "View Cart", cart_path, class: "btn btn-primary btn-sm" %>
#     </div>
#   <% end %>
# </div>